import { Budget } from "@/components/admin/budget";

/* NP7 Experience's budget. Separate company, separate books, separate URL. */
export default function ExperienceBudgetPage() {
  return <Budget world="experience" />;
}
